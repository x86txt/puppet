MAILTO=""
SHELL=/bin/bash
PATH=/bin:/usr/bin:/opt/puppetlabs/bin

# sync latest puppet config from git, apply via puppet
* * * * * cd /etc/puppetlabs/code && /usr/bin/git pull && /opt/puppetlabs/bin/puppet apply /etc/puppetlabs/code/manifests/site.pp
